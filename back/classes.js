class Post {
    constructor(title, date, subtitle, content, images) {
        this.title = title
        this.date = date
        this.subtitle = subtitle
        this.content = content
        this.images = images
    }
}

class Project {
    constructor(name, summary, images) {
        this.name = name
        this.summary = summary
        this.images = images
    }
}